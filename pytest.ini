[pytest]
addopts = -ra
markers =
    positive: happy path checks
    negative: negative checks with invalid data
    corner: corner and idempotency checks
    nonfunctional: basic nonfunctional checks
    contract: response schema and contract checks
    matrix: payload validation matrix checks
    v2: dedicated api v2 checks
